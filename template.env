# BotListBot Configuration
# Copy this file to .env and fill in your values:
#   cp template.env .env

# ── Required ──────────────────────────────────────────────

# Get a bot token from @BotFather on Telegram
BOT_TOKEN=INSERT_BOT_TOKEN

# Your Telegram user ID (message @userinfobot to find it)
DEVELOPER_ID=INSERT_YOUR_TELEGRAM_ID

# PostgreSQL connection (default works with docker-compose up -d)
DATABASE_URL=postgres://botlistbot:botlistbot@localhost:5432/botlistbot

# ── Development ───────────────────────────────────────────

# True = polling mode (local dev), False = webhook mode (production)
DEV=True

# ── Bot Checker (optional) ────────────────────────────────
# The bot checker uses a Pyrogram userbot to ping bots and detect offline status.
# Leave these disabled unless you need this feature.

RUN_BOTCHECKER=False
USE_USERBOT=False
# API_ID=
# API_HASH=
# USERBOT_SESSION=
# USERBOT_PHONE=

# ── Misc (optional) ──────────────────────────────────────

FORBIDDEN_KEYWORDS=must,bot,online,offline,type,start,remove,answer,name,chat,bot,new,send,type,help,set,edit,show,click,change,best,value,read,review,service,short,button,text,list,post,coin,info,forward,level,user,related,left,stat,data,language,time,find,head,like,find,open

# Sentry error tracking (optional)
# SENTRY_URL=
# SENTRY_ENVIRONMENT=
